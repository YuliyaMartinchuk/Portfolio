import React from 'react';
import {Nav} from "../nav/Nav";
import {HeaderContainer} from "./HeaderStyle";

export const Header = () => {
    return (
        <HeaderContainer >
            <Nav/>
        </HeaderContainer>
    );
};

