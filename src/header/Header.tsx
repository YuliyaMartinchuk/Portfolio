import React from 'react';
import {Nav} from "../nav/Nav";
import {HeaderWrapper} from "./HeaderStyle";

export const Header = () => {
    return (
        <HeaderWrapper >
            <Nav/>
        </HeaderWrapper>
    );
};

